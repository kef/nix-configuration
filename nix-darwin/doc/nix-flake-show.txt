~/r/work/nix-configuration/nix-darwin(master) $ nix flake show
warning: Git tree '/Users/kef/r/work/nix-configuration' has uncommitted changes
evaluation warning: Nixpkgs 26.05 will be the last release to support x86_64-darwin; see https://nixos.org/manual/nixpkgs/unstable/release-notes#x86_64-darwin-26.05
evaluation warning: The default value of `programs.neovim.withRuby` has changed from `true` to `false`.
                    You are currently using the legacy default (`true`) because `home.stateVersion` is less than "26.05".
                    To silence this warning and keep legacy behavior, set:
                      programs.neovim.withRuby = true;
                    To adopt the new default behavior, set:
                      programs.neovim.withRuby = false;
evaluation warning: The default value of `programs.neovim.withRuby` has changed from `true` to `false`.
                    You are currently using the legacy default (`true`) because `home.stateVersion` is less than "26.05".
                    To silence this warning and keep legacy behavior, set:
                      programs.neovim.withRuby = true;
                    To adopt the new default behavior, set:
                      programs.neovim.withRuby = false;
evaluation warning: The default value of `programs.neovim.withPython3` has changed from `true` to `false`.
                    You are currently using the legacy default (`true`) because `home.stateVersion` is less than "26.05".
                    To silence this warning and keep legacy behavior, set:
                      programs.neovim.withPython3 = true;
                    To adopt the new default behavior, set:
                      programs.neovim.withPython3 = false;
evaluation warning: The default value of `programs.neovim.withPython3` has changed from `true` to `false`.
                    You are currently using the legacy default (`true`) because `home.stateVersion` is less than "26.05".
                    To silence this warning and keep legacy behavior, set:
                      programs.neovim.withPython3 = true;
                    To adopt the new default behavior, set:
                      programs.neovim.withPython3 = false;
error:
Failed assertions:
- nix-homebrew.enableRosetta is set to true but this isn't an Apple Silicon Mac
error: attribute 'activationPackage' missing
error:
       … while evaluating an attribute for caching

       … while evaluating the attribute 'system.system'
         at «github:LnL7/nix-darwin/a1fa429»/eval-config.nix:88:5:
           87|     inherit (configuration._module.args) pkgs;
           88|     system = configuration.config.system.build.toplevel;
             |     ^
           89|     extendModules = args: withExtraAttrs (configuration.extendModules args);

       … while evaluating the attribute 'optionalValue.value'
         at «github:NixOS/nixpkgs/b5aa0fb»/lib/modules.nix:1310:5:
         1309|
         1310|     optionalValue = if isDefined then { value = mergedValue; } else { };
             |     ^
         1311|   };

       … while evaluating definitions from `/nix/store/6k1rwxwf7b3vs4cwyh65bssbnaiwxrw5-source/modules/system':

       (stack trace truncated; use '--show-trace' to show the full, detailed trace)

       error:
       Failed assertions:
       - nix-homebrew.enableRosetta is set to true but this isn't an Apple Silicon Mac
